# 网瘾学园 · 实施阶段计划 v1.0

> 依据：设计契约 v0.5。契约是唯一设计源；本文件只管顺序、切片范围与阶段门，不重复契约内容。
> 决策输入（2026-07-26 问答）：真机环境已装齐可随时核实；CoT 双方案（A 默认无 / B 可开关）；同代更新模式；垂直切片路线。

## 0. 垂直切片定义

切片目标：用最小内容把「向导 → 行动 → 生成 → 结算 → 镜像」全链路在真机跑通，最早暴露版本敏感风险与掉格式风险。

| 维度 | 切片范围 | 全量范围 |
|---|---|---|
| 少女 | 仅林晚秋（阶段 1–2 占位人设；默认创伤 15，可早测创伤线） | 5 人 × 4 阶段 |
| 行动 | 个别谈话、纪律训练、结束今天（夜晚结算） | 全部 7 项 + 电疗剂量表 |
| 事件 | 首日事件 + 夜晚结算 | 探视日 / 评估日 / 崩溃链 / 结局 |
| shell | 向导（3 项选择）+ 主界面（镜像/顶栏/行动区）+ presentation mode + 逃生开关 | + 少女页 / 回看页 / 设置 |
| CoT | 方案 A 默认态 | + 方案 B 条目组（条件交付） |

切片内 schema 即全量 schema（5 人字段一次建齐，内容后补）——避免切片后迁移。

## 1. 阶段总表

| # | 阶段 | 负责技能 | 主要产出 | 阶段门 |
|---|---|---|---|---|
| 0 | 真机能力核实 | sillytavern-api-reference（+ runtime-debug 取证） | 能力确认表：MVU 方言与命令格式、脚本写入 API、每楼快照/重放能力（§3.5 路径二选一）、世界书条件激活（§3.3 候选 1/2 定案）、createChatMessages · /trigger · 生成事件、同层渲染能力；不支持项的回退决定写回契约 | 契约中全部「经核实」假设关闭，§3.5 重放路径定案 |
| 1 | 切片·状态与结算核心 | 本技能主导（tavern-card-builder 契约 → 实现） | `src/mvu/` 全量 schema + 默认值；`src/bridge/` 结算模块：切片 3 行动的基础增量表、设定块解析、效果系数、夹取/冻结不变量、幂等键、原子失败 | 模拟楼层序列（含 Swipe/编辑/删楼样例）重放一致；双写检查零命中 |
| 2 | 切片·世界书与输出格式（方案 A） | 本技能主导 | `src/lorebook/` 最小集：变量投影、源事件块格式条目（`[mvu_update]`）、玩法规则精简版、林晚秋阶段 1–2 占位条目、文风条目（`[mvu_plot]`）；正则初版（§4.6 契约全覆盖） | 同代上下文 token 实测 ≤ §3.4 方案 A 预算；静态检查表过 |
| 3 | 切片·shell + bridge 界面 | sillytavern-embedded-ui | 开局向导（草稿/预览/确认/防抖）、主界面、busy 链、presentation mode + 逃生开关、GameStateAdapter、结算模块接线 | §4.5 红线全覆盖；刷新后仅凭持久数据重建 |
| 4 | 切片真机联调 | sillytavern-card-pipeline（简装）→ sillytavern-runtime-debug | 切片卡 JSON/PNG；真机记录：向导落库、行动全链路、夜晚结算、Swipe/编辑后重放一致、结算幂等/原子、presentation 降级 | §7「玩法链＋结算脚本」子集通过；**产出掉格式率数据**（→ 是否启用方案 B） |
| 5 | 内容铺量 | character-persona-writer（人设）＋ 本技能（规则/条目） | 5 人 × 4 阶段人设（每份 ≤500 token）+ 专属活动 + 首日事件；全部 7 行动与电疗剂量表入结算脚本；探视日/评估日/崩溃链/结局判定条目与公式；少女页/回看页/设置页 | 人设无脸谱化、阶段过渡有因；全量预算复测过；§3.3 阶段激活机制按阶段 0 定案实装 |
| 6 | 方案 B 完整 CoT（条件交付） | 本技能主导 | 仅当阶段 4/5 掉格式达 §2.6 标准：离线合成完整 CoT 条目组（默认禁用、可开关、标注来源版本） | 开/关两态各自稳定；开态增量 ≤1200 token；关态回到基线无残留 |
| 7 | 全量打包 | sillytavern-card-pipeline | `dist/` 卡.json + 卡.png；载荷一致性校验；卡绑世界书版本同步 | 往返导入无损；dist 与 src 契约一致 |
| 8 | 全量真机验收与调参 | sillytavern-runtime-debug | §7 全清单验收记录（同代全量；额外更新模型路由抽查一次即可）；30 天全程试玩，数值曲线调参（心结节奏、90 AP 经济、压力曲线） | §7 全过；调参结果写回结算脚本增量表并复测 |

## 2. 依赖与并行

```text
0 ──► 1 ──► 2 ──► 3 ──► 4 ──► 5 ──► 7 ──► 8
            │                 ▲      ▲
            └── 5(人设线可自阶段2起并行) ┘
                              6(条件触发，可与5并行) ──► 7
```

- 阶段 0 是硬前置：§3.3 激活机制与 §3.5 重放路径不定案，阶段 1/2 会返工。
- character-persona-writer 的人设线不依赖运行时结论，最早可在阶段 2 起并行；入库（world book 条目化）等阶段 5。
- 阶段 6 是条件阶段：掉格式未达标则跳过，方案 B 不交付空壳。
- 数值调参只发生在阶段 8（真机试玩后），此前一切系数用契约默认值，不提前调。

## 3. 风险与预案

| 风险 | 暴露阶段 | 预案 |
|---|---|---|
| MVU 无脚本写入 API 或无每楼快照 | 0 | §3.5 基线本就是全量重放，降级不阻塞；最坏情形结算结果存 `same_floor_ui` 命名空间并镜像入投影条目（需回写契约再实施） |
| 世界书不支持变量条件激活且脚本开关不可行 | 0 | §3.3 候选 3：单条目+阶段号投影，触发 §3.4 预算重估 |
| 方案 A 掉格式率高 | 4 | 既定预案即方案 B（阶段 6），标准已量化，不临时设计 |
| 同层渲染/事件 API 与 STDB C5 假设不符 | 0/4 | 阶段 4 用切片小成本暴露；契约 §4 按核实结论修订后再进阶段 5 |
| 30 天节奏试玩发现经济失衡 | 8 | 只动结算脚本增量表系数，不动 schema 与条目结构 |

## 4. 当前状态

- [x] 设计契约 v0.5 冻结（本计划的依据版本）
- [ ] 阶段 0 未开始 —— 下一步：调用 sillytavern-api-reference，对本机 ST/酒馆助手/MVU 实际版本核实交接 1 清单
