# 阶段 4 记录 —— 切片真机联调（部分完成，环境阻塞挂起）

> 日期：2026-07-26 ｜ 负责：pipeline 简装 + runtime-debug ｜ 状态：**部分通过；剩余项因浏览器面板不可见挂起**

## 1. 打包产出（简装 pipeline）

| 文件 | 说明 |
|---|---|
| [tools/pack.js](../tools/pack.js) | 打包器：世界书/正则/TH 脚本/身份字段 → chara_card_v3；PNG 生成（纯 Node 零依赖，含 CRC32/deflate）；**载荷回读一致性自检内置** |
| dist/网瘾学园.json | 47.7KB 卡 JSON |
| dist/网瘾学园.png | 76.8KB 含 tEXt chara 载荷 |

格式权威=本机样例卡「交错宙域 MVU2.6.0」逆向结构（chara_card_v3 3.0、character_book 条目全字段、regex_scripts 字段集、tavern_helper.scripts 字段集）。

## 2. 真机已闭合项 ✅

| 用例 | 证据 |
|---|---|
| 卡放入 characters 目录后被端识别 | getCharacters() 后列表 17 项含「网瘾学园·修正日志」（idx 14） |
| 选卡建聊天，0 号楼=first_mes | mesCount 1，mount 依赖文案可读 |
| 内嵌世界书自动导入并绑定 | 弹窗确认后 toast「已成功导入并绑定至角色」 |
| **mount 展开正则生效** | 0 号楼 DOM 出现 `pre` 代码块（[[WYXY_SHELL_MOUNT]] → html 块替换成功，$ 转义无恙） |
| 授权路径确认 | 角色域正则需入 `character_allowed_regex`；TH 卡内脚本需入 `tavern_helper.script.enabled.characters`（按**角色名**）——两者已为本卡写入用户设置 |

## 3. 挂起项（环境阻塞：浏览器面板不可见）

阻塞机理：面板隐藏 → 页面无合成 → rAF 冻结/定时器节流 → ST 前端 boot 与 TH 渲染器（代码块→iframe）无法推进。Claude in Chrome 扩展亦未连接，无替代 headed 浏览器。

- [ ] 代码块 iframe 化（shell 实际渲染）与向导交互全链
- [ ] TH 卡内脚本执行（MVU 双源加载器、bridge）→ window.Mvu 出现
- [ ] 开局链：确认→真实楼层→/trigger 生成→[InitialVariables] 初始化→结算钩子
- [ ] VARIABLE_UPDATE_ENDED 触发时序（Swipe/编辑/删楼）与结算幂等
- [ ] presentation mode 折叠/逃生/心跳恢复
- [ ] 窄容器布局
- [ ] 掉格式率数据（→ 方案 B 决策，暂按未触发处理）

**恢复条件**：浏览器面板被用户展开（或 Claude in Chrome 连接）。恢复后从「重载 127.0.0.1:8000 → 选卡 → 完整跑 §7 切片子集」继续；授权已持久化，无需重做。

## 4. 对计划的影响

- 阶段 5（内容铺量，纯本地）不依赖本阶段结论，先行推进。
- 阶段 6（方案 B）决策依赖掉格式率——数据缺失期间按「未达标=不交付」处理，真机恢复后复核。
- 阶段 8 全量验收必须先补完本阶段挂起项。
