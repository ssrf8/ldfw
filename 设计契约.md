# 网瘾学园 · 修正日志 —— 设计契约 v0.6

> 状态：设计阶段（未实现）。本文件是唯一设计源，实现与打包不得绕过此契约。
> v0.3（制卡技能评审后）：补齐夜晚结算触发、开局初始化契约、CoT 增量声明表、分阶段人设激活机制、提示词预算门、正则契约、依赖缺失降级、产物分发决策；修正账本缺字段与病倒态无产生机制。
> v0.4（写者重构）：数值与规则字段全部移交本地结算脚本（确定性重放基线），LLM 只写故事关键语义（心结/状态/秘密揭示）与行动效果类别；新增 §3.5 结算脚本契约；CoT 增量与更新模型上下文相应减负。
> v0.5（规划问答后）：CoT 改双方案交付——A 默认无自定义 CoT / B 可开关完整版（掉格式触发启用）；实际运行模式定为同代更新（双兼容标记保留）；开发采用垂直切片，见《阶段计划.md》。
> v0.6（阶段 0 真机核实后，见 记录/阶段0）：结算脚本挂 `Mvu.events.VARIABLE_UPDATE_ENDED` 原地计算（重放降级为对账工具）；阶段激活定案候选 2（bridge 切条目 enabled）；MVU 框架经卡内加载器双源 CDN 引入，零外网范围收窄；脚本写 API 落地为 `Mvu.replaceMvuData`。

## 0. 决策记录

| 决策 | 结论 |
|---|---|
| 卡片类型 | 混合卡：MVU 变量 + 世界书 + 正则 + **真·同层前端**（按 STDB C5 定义：首楼闭合完整游玩主链，三层架构 shell/bridge/真实楼层） |
| 少女阵容 | 固定 5 人（对象存储，非动态 record） |
| 回合结构 | 每日 3 行动点（AP）。夜晚结算是一次显式 PlayerAction：AP 归 0 后行动区仅留「结束今天」，点击发送结算行动走 §4.2 主链 → 日期 +1、AP 重置。禁止 shell 自动代发消息；AP>0 时「结束今天」亦可用（弃置剩余 AP，需二次确认） |
| 开场策略 | 首楼 shell 内开局向导路由（草稿态 → 确认 → 拼一条正式用户开场消息走正常生成链，禁止脚本伪造初始化） |
| CoT 策略 | 双方案交付（§2.6）：**方案 A 默认无自定义 CoT**——输出可靠性靠源事件块格式条目 + 脚本容错；**方案 B 完整 CoT 随卡交付但默认禁用**——真机观测「掉格式」达标准时启用，不依赖任何预设。条件模块（探视/评估/崩溃）是内容条目，两方案下都存在。数值记账与方向校验在结算脚本，CoT 只管叙事一致性与效果类别 |
| MVU 更新路由 | 新卡按双兼容布局设计：变量更新规则与输出格式条目名含 `[mvu_update]`；剧情 CoT / 文风 / 玩法叙事条目名含 `[mvu_plot]`；仅两模型都需要的条目（当前变量表投影）不打标记。运行时是否支持条目路由需经 api-reference 核实，不支持则自然退化为普通条目。**实际运行模式：同代更新**（标记在同代下无副作用，保留双兼容为未来选项） |
| 写者分工 | v0.4 分工：**结算脚本（bridge 内结算模块，§3.5）是全部数值与规则字段的唯一写者**（修正/信任/创伤/AP/day/压力/结局/电疗标记/难度系数/初始关注/schema_version）；**LLM 只写故事关键语义字段**（心结、状态、标记.秘密揭示）并输出行动效果类别等语义源事件。每个字段唯一写者（`标记` 按键分写者），禁止竞写；账本见 §3。 |
| 内容边界 | 全部角色均为成年人（18+）。高压手段（禁闭/电疗）作为被批判的机制存在于玩法中：短期刷高表面数值、长期导向坏结局。警示意味由机制本身传达，不做美化。 |
| 输出与文风 | 见 §2.5：第二人称面向园长、正文+隐藏更新块双段结构、不代打{{user}} |
| 产物分发 | shell/bridge 构建产物**全部内嵌卡内**；MVU 框架跟随生态标准由卡内加载器**双源 CDN** 引入（jsdelivr 国外/testingcf 国内，`MagicalAstrogy/MagVarUpdate/artifact/bundle.js`），零外网范围=除此之外无网络请求；0 号 mount 标记兼作依赖说明的可读文案（§4.7） |
| 目标运行时 | 已核实（记录/阶段0）：luker v2.7.0（ST 1.18 系）+ 酒馆助手 4.8.18 + MVU Zod（CDN bundle，`[mvu_update]` v1.9.5 规则基线）+ ST-Prompt-Template 1.17.4（备选）。更新方式=随AI输出（同代）。 |

## 1. 主题与警示内核（双轨数值）

玩法核心是**「修正值」与「真实成长」的双轨背离**：

- **修正值**（0–100）：机构 KPI，家长和园方只看这个。表面服从度。
- **信任**（0–100）+ **心结进度**（0–4 阶段）：真实成长线，只能靠倾听、陪伴、兴趣引导缓慢积累。
- **创伤**（0–100）：高压手段的隐性代价，面板上默认弱化显示（讽刺：机构报表里没有这一栏）。

高压路线（纪律训练/禁闭/电疗）→ 修正值涨得快、信任跌、创伤涨 → 评估日拿到「假性修正」结局：出园即复发，附家长感谢信与后日谈的强烈反差（警示文本）。
怀柔路线 → 修正值涨得慢、家长施压事件变多（压力来源），但解开心结后修正值会自然跃升 → 「蜕变」真结局。
创伤 ≥ 80 → 崩溃事件链（坏结局，不可逆），是对现实网戒机构的直接警示。

玩家的两难 = 家长满意度倒逼 vs 少女真实需要。这是本卡的戏剧引擎。

## 2. 内容地图

### 2.1 舞台

「青禾修正学园」：民办网瘾矫治机构。设施：宿舍、教室、谈话室、活动室、惩戒室（电疗室，常年上锁但钥匙在{{user}}手里）。周期：30 天修正期，第 30 天总评估日；每 7 天一次家长探视日（检查修正值）。

### 2.2 角色（占位人设，交 character-persona-writer 深化）

均为成年人（18–22），被父母以「为你好」名义送入：

| key | 姓名(占位) | 入园原因 | 核心心结方向 |
|---|---|---|---|
| linwanqiu | 林晚秋 | 自卑内向社恐，网络是唯一出口，父母认定「有病」 | 被否定的自我价值 |
| jiangling | 江铃 | 电竞打游戏，「不务正业」 | 被没收的梦想与天赋 |
| sunian | 苏念 | 厌学拒学，被送来「改造」 | 厌学的真实原因（校园/家庭） |
| fangtangtang | 方糖糖 | 直播短视频成瘾，昼夜颠倒 | 用喧闹掩盖的孤独 |
| guyunlei | 顾云蕾 | 沉迷网恋与网文，被认定「精神不正常」 | 现实中从未被爱过 |

每人四阶段人设：抵触 → 试探 → 敞开 → 蜕变（或崩溃分支），按心结进度切换世界书投影。

### 2.3 行动菜单（每次消耗 1 AP）

| 行动 | 修正值 | 信任 | 创伤 | 备注 |
|---|---|---|---|---|
| 个别谈话（选人） | — | + | — | 推进心结的主要途径 |
| 陪同活动/兴趣引导（选人） | +小 | ++ | — | 按人设有专属活动 |
| 团体课程 | +小(全员) | +微 | — | 低效但安全 |
| 纪律训练 | ++ | − | +小 | 机构标准流程 |
| 禁闭 | +++ | −− | ++ | 触发目标状态「低落/抵触」 |
| 电疗（可选剂量） | ++++ | −−− | +++(随剂量) | 解锁需家长压力≥阈值；使用后有不可逆事件标记 |
| 处理园务文书 | — | — | — | 家长压力 −，恢复回旋余地 |

家长压力变动规则（数值可调，判定与写入规则入 `[mvu_update]` 条目）：

- 探视日结算：全员修正值均值 ≥50 → 压力 −10；<50 → 压力 +15。
- 处理园务文书：压力 −10。
- 任一少女崩溃：即时 +30，并进入机构曝光支线判定。
- 其余行动不直接改压力（修正值间接影响探视日结算）。

数值结算分工（v0.4）：

- 上表增减号是**基础增量表**，由结算脚本持有并计算：Δ = 基础增量 × 难度系数 × 效果系数，写入前夹取 0-100。LLM 不输出这些数字。
- **行动效果类别**由剧情模型按叙事结果输出（语义源事件）：显著 ×1.5 / 一般 ×1.0 / 受阻 ×0.5；缺失或非法时默认「一般」，不阻塞链路。
- 电疗剂量表、家长压力公式、夜晚结算与结局阈值判定全部归结算脚本（§3.5）。

### 2.4 结局判定（第 30 天评估，逐人结算）

- **蜕变**：信任 ≥ 70 且心结 = 4 —— 真结局，后日谈正向。
- **假性修正**：修正值 ≥ 70 且信任 < 40 —— 家长满意，后日谈揭示复发（警示）。
- **崩溃**：创伤 ≥ 80 即时触发，不等评估日（警示，含机构被曝光支线）。
- **未完成**：其余情况，平淡离园，留白后日谈。
- 全员结局组合决定{{user}}的园长线总结局（改革者/共犯/被吞噬）。
- 不变量：`结局` 一旦非 null 即冻结，后续更新不得改写（不可逆）。
- 阈值判定与结局写入由结算脚本执行；剧情模型只演出结局叙事（`[mvu_plot]`，§3.1）。
- 数值曲线（心结每阶所需谈话次数、行动收益系数）不入契约，真机试玩后调参；契约只锁结构与阈值语义。

### 2.5 输出与文风契约

- 视角：第二人称面向{{user}}（园长）；不代{{user}}决策、发言或行动。
- 每回合输出结构：剧情正文 → 语义源事件块（效果类别 + 心结/状态/秘密揭示更新，显示层由正则隐藏，§4.6）。正文不使用代码块/表格等重排版——首楼镜像区按纯文本+轻强调渲染。
- 正文长度基线 300–800 字；夜晚结算、探视日与结算日可放宽至 1200 字。
- 高压行动的后果描写遵循 §0 内容边界：呈现代价，不美化。

### 2.6 CoT 方案（A 默认无 / B 可开关完整版）

- **方案 A（默认交付态，无自定义 CoT）**：输出可靠性依赖三件事——§2.5 文风契约、源事件块输出格式条目（精简，含一个不可照抄的结构示例）、结算脚本容错（效果类别缺失降级、解析失败原子放弃）。
- **方案 B（掉格式保险）**：一份完整 CoT，由基线七阶段（input_check / scene_state / character_state / goal_conflict / action_selection / continuity_check / output_format）与下表增量按制卡技能拼接规则**离线合成**（标注来源版本），作为默认禁用的世界书条目组随卡交付；启用即整体生效，不依赖任何预设。
- **启用标准（可调）**：真机连续 20 轮内源事件块缺失或无法解析 ≥3 次，或正文反复泄漏系统结论 → 打开方案 B。
- 下表是方案 B 的合成源；未来若接入带主 CoT 的预设，它同时充当增量声明表，不另写。

| 增量/模块 ID | 激活 | 目标阶段·位置 | 接收模型 | 输出边界 | 跳过条件 |
|---|---|---|---|---|---|
| wy.ap_accounting.v1 | 常驻 | action_selection·后 | 剧情 | 仅供判断（AP/日程由脚本记账；只负责把消耗与疲惫感融入正文、AP 归 0 时收束当日） | 非行动回合（向导确认前） |
| wy.dual_track.v1 | 常驻 | continuity_check·后 | 剧情 | 仅供判断（后果描写须与行动效果类别、双轨主题一致；数值方向校验已移交结算脚本） | 无数值变动的纯对话回合 |
| wy.visit_day.v1 | 条件：day%7=0 | scene_state·后 | 剧情 | 探视事件允许输出 | 非探视日 |
| wy.ending_gate.v1 | 条件：day=30 或任一创伤≥80 | goal_conflict·后 | 剧情 | 结算叙事允许输出 | 条件不成立 |
| wy.collapse_chain.v1 | 条件：任一创伤≥80 | —（条件内容条目组，非 CoT） | 剧情 | 允许输出 | 未触发 |

- 语义字段更新与源事件块格式属 `[mvu_update]` 规则；数值上限与公式在结算脚本内（§3.5）。两者都不进 CoT。效果类别是允许输出的唯一系统结论（写入源事件块，显示层隐藏）。
- 降级：方案 B 关闭或缺失即回到方案 A 基线（卡片身份 + 变量投影 + §2.5 约束 + 源事件格式），玩法链路不依赖 CoT 存活；条件模块不触发时零输出（不写「本模块未触发」）。

## 3. 状态地图（MVU 字段账本）

方言与命令格式实现前经 api-reference 核实，此处只定契约。

```
stat_data
├─ world
│  ├─ day            int  默认1    写:脚本(夜晚结算)  读:模型/UI  30天期
│  ├─ ap             int  默认3    写:脚本(行动扣1/结算重置3)  读:模型/UI  归0→仅留「结束今天」
│  ├─ parent_pressure int 默认20   写:脚本(§2.3公式)  读:模型/UI  0-100,≥60解锁电疗并触发施压事件
│  ├─ 难度系数        num  默认1.0  写:脚本(仅开局,解析设定块)  读:脚本  行动收益乘数，开局后只读
│  └─ 初始关注        str|null 默认null 写:脚本(仅开局,解析设定块)  读:模型  首日专属事件目标
├─ girls.<key> ×5（固定对象，不增删）
│  ├─ 修正值   int 0-100  默认见各人初值   写:脚本(基础增量×难度×效果,夹取)  读:模型/UI
│  ├─ 信任     int 0-100  默认10-25按人设  写:脚本(同上)  读:模型/UI
│  ├─ 创伤     int 0-100  默认0(林晚秋15)  写:脚本(同上)  读:模型/UI(仅详情页)
│  ├─ 心结     int 0-4    默认0    写:模型(语义判断,单次±1)  读:模型/UI/世界书阶段激活(§3.3)
│  ├─ 状态     str        正常/低落/抵触   写:模型(语义)  读:模型/UI  （崩溃不入此枚举，由 结局=崩溃 表达）
│  ├─ 标记     obj        电疗过:bool 写:脚本(行动触发,单向)；秘密揭示:bool 写:模型（按键分写者；新增键必须补账本行；未知键由打包期 schema 校验拒绝）
│  └─ 结局     str|null   默认null  写:脚本(阈值判定:崩溃即时/评估日结算)  读:模型/UI  写后冻结
└─ meta.schema_version int 默认1   写:脚本(迁移)  迁移用
```

- 单次行动数值变动上限（修正值±15、信任±10、创伤±20，电疗按剂量表）由基础增量表×效果系数在脚本内天然保证，无需模型自律。
- 不变量由结算脚本强制：0-100 写入夹取、`结局` 非 null 后冻结、`标记.电疗过` 只能 false→true。
- LLM 显式变量输出缩减为：心结、状态、秘密揭示、行动效果类别（对照制卡技能「混合～重计算系统卡」经验带 10–50%）。
- 无消费者的字段不得入库；新增字段必须补账本行（写者/读者/渲染/清理）。
- 迁移：v0.4 仅预留 `schema_version`；迁移逻辑由结算脚本承担（写者已登记），须幂等。

### 3.1 更新路由（双兼容布局）

| 世界书条目 | 路由标记 | 说明 |
|---|---|---|
| 当前变量表投影 | 无标记 | 剧情模型与更新模型都需要（唯一合理的共享条目） |
| 变量更新规则 / 输出格式 | `[mvu_update]` | 仅更新模型需要 |
| 玩法规则 / 剧情 CoT 增量 / 文风 / 少女人设 / 结局判定叙事 | `[mvu_plot]` | 仅剧情模型需要，不进更新模型上下文 |

同代更新模式下标记不生效、自然退化为普通激活条目；额外更新模型模式下按上表分流。标记可出现在条目名任意位置。每个无标记条目必须在本表登记共享理由。
v0.4 写者重构后 `[mvu_update]` 只含：语义字段（心结/状态/秘密揭示）更新规则 + 语义源事件块输出格式。数值公式、阈值判定与结局写入在结算脚本内，不进任何模型上下文；`[mvu_plot]` 的「结局判定叙事」只管演出，不管落库。
阶段 2 落地（依据装机声明与样例卡实证）：LLM 更新方言用 TH 声明的 `_.set(path, 旧值, 新值); //原因` 原生命令族（不引入 JSONPatch/Zod schema 脚本）；初始化用 `[InitialVariables]` 条目（disable 状态下 MVU 仍按名读取）；投影条目命名 `[mvu_context]`（跟随装机 MVU 惯例）、内容为 EJS `@@activate` 模板（ST-Prompt-Template）。

### 3.2 初始化契约（开局即普通一轮）

- schema 默认值即「未开局安全态」；向导未完成前 shell 不发任何行动。
- 向导确认后拼接的开场消息内含**结构化设定块**（人类可读、更新模型可提取），例：`【开局设定】理念=怀柔；难度=标准；首位关注=林晚秋`。
- 结算脚本解析设定块并确定性写入：`难度系数`、`parent_pressure` 初值偏移、全员`信任`偏移、`初始关注`（脚本是这些字段的账本写者；解析真实楼层里的设定块是确定性结算，不属于「脚本伪造初始化」——被禁止的是绕过真实楼层的低层状态替换）。剧情模型只演出开局，不落库这些数字。
- 初始化落库正确性是 §7 验收项。

### 3.3 分阶段人设条目激活

每位少女的四阶段人设为 4 个互斥世界书条目，按该少女 `心结` 值条件激活，同一时刻每人恰好 1 条激活。

- **定案（阶段 0）**：bridge 在心结变更后经 `updateWorldbookWith` 切换条目 `enabled`（世界书开关不是 stat_data 写入，不违反单写者账本；职责已登记 §4 运行时地图）。条目激活策略用蓝灯（constant）+ enabled 开关，不依赖关键字扫描。
- 备选：EJS 条件渲染（ST-Prompt-Template 1.17.4 已装）——条目常开、内容按 `心结` 条件渲染；仅当 enabled 切换在真机出现时序问题时启用。

### 3.4 提示词预算门

| 上下文 | 常驻内容 | 预算目标（目标模型 token） |
|---|---|---|
| 剧情模型（同代·方案 A 默认态） | 变量投影 + 玩法规则 + 源事件格式 + 5×当前阶段人设 + 文风 | ≤ 3500；其中每份阶段人设 ≤ 500 |
| 方案 B 启用时的增量 | + 完整 CoT 条目组 | + ≤ 1200（技能复杂系统卡经验带上限） |
| 更新模型（额外模式，当前不启用） | 变量投影 + 语义字段更新规则 + 源事件格式 | ≤ 800（保留双兼容，仅启用时适用） |

条件模块（探视/评估/崩溃）不计入常驻。实现期用目标模型 tokenizer 实测复核；超预算先按语义去重、再删减措辞，不得为省 token 把条目错发给另一模型。

### 3.5 结算脚本契约（bridge 结算模块）

- 输入（全部确定性可重放）：PlayerAction 元数据（行动类型/目标/剂量，存 `same_floor_ui`）、AI 楼层中的语义源事件块（效果类别 + 心结/状态/秘密揭示更新）、开场消息设定块。
- 职责：按 §2.3 公式计算并写入全部脚本写者字段；强制夹取与冻结不变量；探视日/夜晚/评估日结算；崩溃阈值即时判定。
- 触发（阶段 0 定案）：挂 `Mvu.events.VARIABLE_UPDATE_ENDED`，在回调 `(variables, variables_before_update)` 上**原地计算派生字段**——随 MVU 每楼更新生命周期执行，Swipe/重生成一致性由构造保证（MVU 按楼重解析→钩子重跑）；楼外修正兜底用 `Mvu.replaceMvuData`。事件常量必须经 `Mvu.events.*` 引用（存在历史拼写）。「确定性重放」（由设定块+行动序列+源事件从头重算）降级为对账/自检工具，不再是运行路径。
- 幂等：同一楼层重复触发只生效一次（以 action_id / 楼层号为结算键）。
- 失败原子性：解析或写入失败即整笔放弃（无半结算），解锁 busy 并在 shell 报错横幅提示；效果类别缺失/非法降级为「一般」，不阻塞。
- 语义源事件块在显示层由正则隐藏（§4.6），与变量更新块同规则。

## 4. 运行时地图（真·同层前端，事实基线：STDB C5）

架构定义遵循 STDB 知识库 `C:\Users\Administrator\.cc-switch\skills\STDB\C5_同层前端.md`：完整游玩主链闭合在首楼（0号消息）应用内；真实 user/AI 楼层照常生成，承担上下文、Swipe、分支与存档，由首楼镜像。

### 4.1 三层架构

```text
真实消息数据层（ST chat：0=shell / 1=user / 2=assistant / …）
  ↑↓ Tavern Helper API / events
桥接服务层（常驻脚本）：事件汇聚、消息读取、生成状态、显示适配、产物加载、shell 重建后重新接线
  ↑↓ 共享接口 / 自定义事件
首楼应用层（0号消息 iframe shell）：路由、渲染、输入、回看、设置
```

- 0 号消息正文只放**短 mount 标记**，不写完整剧情镜像；应用由显示期展开 + 桥接脚本维持。
- 单向数据流：真实聊天/MVU → bridge 归一化 snapshot → shell store → 组件渲染 → 玩家 action → 真实楼层 → bridge 重读。所有楼层 API 调用收口到 chat-service / game-state-adapter，组件不得直接调楼层 API。

### 4.2 回合主链路（唯一推荐链）

```text
玩家在首楼点行动按钮/输入
→ shell 校验（AP、选人）并生成 action_id
→ createChatMessages 创建真实 user 楼层（正文=人类可读行动描述，元数据入 data.same_floor_ui）
→ /trigger 触发 ST 正常生成 → 真实 assistant 楼层 → 语义字段按 MVU 普通管线解析
→ 结算脚本读行动元数据 + 源事件块，计算并写入派生字段（§3.5）
→ bridge 收事件重读 snapshot → 首楼镜像剧情与数值
```

- 禁止用 `generate()/generateRaw()` 替代正常回合（不建楼层、MVU 不解析、产生两套真相）。
- busy 解锁以生成结束/停止事件 + 真实消息重读为准，备事件漏收超时对账；事件用 debounce + 单飞锁聚合。
- 按钮与自由输入统一为 PlayerAction；正文保留可回看语义，不发纯控制码。

### 4.3 shell 界面结构（单 shell 内部路由）

- **主界面**：剧情镜像区（最新 assistant 正文）+ 顶栏（Day X/30 · AP ●●○ · 家长压力条）+ 行动区（7 项行动按钮，选人弹 Modal；AP=0 置灰仅留「结束今天」）。
- **少女页**：5 张卡片，修正值/信任双条；创伤默认仅小图标暗示，点开详情才见（呼应「报表里没有这栏」）。
- **回看页**：镜像真实楼层（含全部 Swipe 读取与切换），不是复制存档。
- **开局路由**：shell 首次启动进入开局向导——管理理念（铁腕/平衡/怀柔→初始家长压力与全员信任）、难度（涨幅系数入 world）、初始关注对象（首日专属事件）；草稿→预览不写状态→确认冻结→拼一条规范用户开场消息走 §4.2 主链完成初始化。禁止低层状态替换模拟初始化。

### 4.4 状态四分类（不得混放）

| 状态 | 本卡内容 | 位置 |
|---|---|---|
| 真实聊天 | 行动消息、剧情回复、Swipe | ST 消息楼层 |
| 游戏状态 | stat_data（§3 账本） | MVU 后端；写者=结算脚本+模型按 §3 账本分工；shell 经 GameStateAdapter 只读，普通操作不直写 stat_data |
| 可恢复 UI 状态 | 当前路由、行动草稿、回看锚点 | `same_floor_ui` 命名空间（chat 变量或 0 号楼 data），带 protocol_version |
| 本机偏好 | 主题、字号、动效 | localStorage，带卡片+版本前缀 |

iframe 内 store 只是缓存；刷新后 shell 必须仅凭持久数据重建界面。

### 4.5 显示模式（最高优先级红线）

- **严禁**把后续真实楼层设 `is_hidden: true`（改变提示词语义），严禁删除/清空/改写真实消息。
- 「玩家只看首楼」用展示适配层实现：bridge 在 shell 健康时开启项目命名空间的 presentation mode，仅视觉折叠非 shell 消息容器；须有「一键显示原生消息」逃生开关；shell 心跳丢失/出错/切聊天时自动恢复原生显示；选择器失效安全失败为「都可见」。
- 单层模式前提：shell 内提供可键盘导航的完整回看、说话者语义、新消息通知。

### 4.6 正则

- 目标：隐藏模型输出中的语义源事件块、变量更新块与渲染标记（具体标签名随实现确定）。
- 契约：仅作用于 AI 消息显示层，不改楼层字段；流式与最终态行为一致（流式中的半截更新块不得闪现明文，允许占位替代）；幂等（重复应用结果不变）；不匹配时安全失败为原样显示，不得吞正文；禁止用正则修补变量或提示词协议错误。

### 4.7 依赖缺失降级

- 硬依赖：酒馆助手 + MVU 扩展。0 号消息的短 mount 标记同时兼作可读降级文案（说明本卡依赖与安装指引）。
- 无酒馆助手：shell 不渲染，原生楼层照常可见，不报错、不锁死聊天。
- 有助手无 MVU：bridge 探测失败即阻止进入开局向导并给出提示，不产生半初始化状态。

## 5. 工件地图

```
网瘾学园/
├─ 设计契约.md          ← 本文件（源）
├─ 阶段计划.md          ← 实施阶段计划（垂直切片路线）
├─ src/
│  ├─ card/            识别字段、description、开场消息
│  ├─ lorebook/        世界观 / 玩法规则 / 变量更新规则 / 结局判定 / 少女×5(分阶段)
│  ├─ mvu/             schema 与初始化
│  ├─ shell/           首楼应用源码（路由/组件/store/GameStateAdapter，构建产物内嵌入卡、经短 mount 加载，见 §0 分发决策）
│  ├─ bridge/          桥接常驻脚本源码（事件汇聚/显示适配/产物加载）
│  └─ regex/
└─ dist/               生成物：卡.json / 卡.png（永不手改）
```

## 6. 专家交接清单

| 顺序 | 技能 | 输入 | 产出 | 验收门 |
|---|---|---|---|---|
| 1 | sillytavern-api-reference | 目标 MVU 方言、`[mvu_plot]`/`[mvu_update]` 条目路由支持、同层渲染与按钮发消息 API、分阶段条目条件激活能力（§3.3 候选 1/2）、预设主 CoT 拼接点（§2.6 假设）、MVU 脚本写入 API 与每楼变量快照/重放能力（§3.5） | 确认的签名与能力清单 + 不支持项的回退选择 | 与目标版本一致 |
| 2 | character-persona-writer | §2.2 占位人设 + 四阶段要求 | 5 份分阶段人设 | 无脸谱化、阶段过渡有因 |
| 3 | sillytavern-embedded-ui | §4 全部契约 + §3.5 结算脚本契约 + STDB C5/C10/C1 | shell + bridge（含结算模块）实现 | §4.5 红线、事务规则与 §3.5 幂等/原子性全覆盖 |
| 4 | sillytavern-card-pipeline | src/ 全量 | dist/ JSON+PNG | 载荷一致性校验 |
| 5 | sillytavern-runtime-debug | dist/ 卡 | 真机验收记录 | §7 全过 |

## 7. 真机验收清单

玩法链：

- 开局向导：提交、失败重试、重复提交防抖、重载后草稿恢复。
- AP 链：3 次行动 → AP=0 仅留「结束今天」→ 点击发送结算行动 → day+1、ap=3、事件正确；AP>0 提前结束当天的二次确认与弃置逻辑正确。
- 行动按钮 → 真实 user 楼层 → /trigger 生成 → 语义字段 MVU 解析 + 脚本结算派生字段 → 首楼镜像刷新全链路。
- 家长探视日、电疗解锁与不可逆标记、崩溃即时触发。
- 第 30 天逐人结算 + 园长总结局。
- 新聊天/旧聊天重载后状态可读（schema_version 迁移路径）。

路由、初始化与降级：

- 同代更新模式全量验收（当前运行模式）；额外更新模型模式为可选项——条目路由抽查一次（无标记 / `[mvu_plot]` / `[mvu_update]` 接收方与 §3.1 一致），不阻塞发布。
- CoT 双方案：方案 A 默认态跑全部主验收；模拟掉格式场景验证方案 B 开启后格式恢复、关闭后回到基线；按 §2.6 标准记录源事件块缺失率。
- 初始化：开局设定块 → 难度系数 / 压力偏移 / 全员信任偏移 / 初始关注全部落库正确。
- 分阶段人设：心结推进后旧阶段条目停用、新阶段条目激活，同一时刻每人恰好 1 条。
- 正则：流式中与最终态均不闪现更新块明文；构造不匹配样例验证安全失败（原样显示、不吞正文）。
- 依赖缺失：无酒馆助手 / 无 MVU 两场景按 §4.7 降级。
- 预算：实测剧情与更新两侧上下文 token，与 §3.4 预算门对账。
- 不变量抽查：0-100 夹取、结局冻结、电疗标记不可逆在长局中未被违反。
- 长局漂移：连续推进 ≥15 天，抽查 CoT 约束与数值上限仍被遵守。
- 零外网：全程无网络请求（产物内嵌）。

结算脚本（§3.5）：

- 双写检查：抽查全部字段，无一字段同轮被模型与脚本同时写入。
- 幂等：同一楼层重复触发结算只生效一次。
- 重放：Swipe / 编辑 / 删楼 / 重生成后派生数值与从头重算结果一致，无两套真相。
- 原子性：构造源事件解析失败样例 → 无半结算、busy 正常解锁、错误横幅可见。
- 降级：效果类别缺失或非法 → 按「一般 ×1.0」结算，链路不断。
- 初始化：设定块由脚本解析落库，与向导确认页预览一致。

同层专项（对照 STDB C5 §14）：

- 刷新/切聊天/重绘后 shell 仅凭持久数据完整重建（路由、草稿、回看锚点）。
- 回看页镜像全部 Swipe，切换 Swipe 后上下文与数值随之正确。
- 编辑/删楼/重生成后回看索引全量重建，无两套真相。
- presentation mode：逃生开关有效；shell 出错时自动恢复原生显示；无任何真实楼层被写 is_hidden。
- 生成中断/停止/事件漏收：超时对账解锁，无重复提交。
- 移动端布局与键盘导航可用。
